# 开发规范

## 命令

```bash
npm run build
npm run lint
npm run format:check
npm test
```

## 中文注释要求

- 新增代码必须在关键业务逻辑旁同步补充中文注释。
- 任务状态流转、计费预占/结算/释放、权限判断、幂等处理、重试、对账、文件访问、AI 网关调用必须写中文注释。
- 注释要解释业务意图、数据流或异常处理原因，不写“给变量赋值”这类无信息量注释。
- 敏感配置、密钥、连接串、Token 不能写入注释、日志或测试快照。
